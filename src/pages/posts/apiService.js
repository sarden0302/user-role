// axios. 작성했던 기능을 모아서 설정한 다음 각 jsx 파일로 전달


// port 환경변수 설정
//                          스프링부트 실행 포트 RestController 에서 RequestMapping 에 작성한 api 를 그대로 작성
const API_POST_URL = "http://localhost:8080/api/posts";

const apiService = {
    // 외부에서 사용할 메서드 명칭 기능설정 (파라미터 값) {기능작성}
}